'use client';
import { useEffect, useState } from 'react';
import { Command, HardDrive, ChevronRight, Keyboard, Circle } from 'lucide-react';
import { useDesktop } from '@/lib/state/store';
import { oma } from '@/lib/oma/bus';
import { fs } from '@/lib/fs/opfs';
export default function Bar(){const s=useDesktop(),[clock,setClock]=useState(''),[storage,setStorage]=useState('—');useEffect(()=>{const update=()=>{const d=new Date();setClock(d.toLocaleDateString('en-GB',{weekday:'short',day:'2-digit',month:'short'})+'  '+d.toLocaleTimeString('en-GB',{hour:'2-digit',minute:'2-digit'}));};update();const interval=setInterval(update,1000);return()=>clearInterval(interval);},[]);useEffect(()=>{void navigator.storage?.estimate().then(e=>{const bytes=e.usage??0;setStorage(bytes<1048576?Math.max(1,Math.round(bytes/1024))+' KB':(bytes/1048576).toFixed(1)+' MB');});},[s.fsVersion]);const focus=s.workspaces[s.workspace].focus,tile=focus?s.tiles[focus]:null;return <header className="bar"><div className="bar-left"><button className="system-button" aria-label="Open system menu" title="System menu · Alt+Shift+Space" onClick={()=>s.setOverlay('menu')}><Command size={15}/></button><nav aria-label="Workspaces">{Array.from({length:9},(_,i)=>i+1).map(n=><button key={n} aria-label={`Workspace ${n}`} aria-current={s.workspace===n?'page':undefined} className={'workspace-button '+(s.workspace===n?'active':s.workspaces[n].layout?'occupied':'')} onClick={()=>void oma(['ws',String(n)],{store:useDesktop,fs})}>{n}</button>)}</nav><span className="bar-divider"/><button className="wordmark" onClick={()=>s.setOverlay('launcher')} title="Launcher · Alt+Space">oma.os</button><span className="focused-class">{tile?.app==='term'?'terminal':tile?.app}{focus&&s.dirty[focus]&&' *'}</span></div><time>{clock}</time><div className="bar-right"><button className="bar-agent" onClick={()=>void oma(['launch','agent'],{store:useDesktop,fs})}><ChevronRight size={13}/><span>agent</span><span className="muted">offline</span></button><span className="bar-divider"/><span className="storage" title="Browser storage used"><HardDrive size={12}/>{storage}</span><button className="theme-button" onClick={()=>s.setOverlay('theme')}><Circle size={8}/>tokyo-night</button><button className="key-button" title="Keyboard shortcuts · Alt+K" aria-label="Keyboard shortcuts" onClick={()=>s.setOverlay('keys')}><Keyboard size={14}/></button></div></header>}
