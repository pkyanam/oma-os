export function parse(input:string):string[]{const args:string[]=[];let word='',quote='',started=false,escape=false;for(const ch of input){if(escape){word+=ch;escape=false;started=true;continue;}if(ch==='\\'&&quote!=="'"){escape=true;started=true;continue;}if(quote){if(ch===quote)quote='';else word+=ch;continue;}if(ch==='"'||ch==="'"){quote=ch;started=true;continue;}if(/\s/.test(ch)){if(started){args.push(word);word='';started=false;}}else{word+=ch;started=true;}}if(quote)throw new Error('Unclosed quote');if(escape)word+='\\';if(started)args.push(word);return args;}
